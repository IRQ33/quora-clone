# Blog

## How to run

1. Create `application.yaml` in `src/main/resources`
2. Copy configuration from `application-example.yaml`
3. Fill in your own credentials (Google OAuth, passwords, etc.)
4. Run the application